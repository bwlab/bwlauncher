{% extends '@PrestaShop/Admin/layout.html.twig' %}
{# TODO: Edit #}
{% set layoutTitle = 'Launcher module'|trans({}, 'Modules.Bwlauncher.Admin') %}

{% block content %}
    {{ form_start(form) }}
    <div class="card">
        <h3 class="card-header">
            <i class="material-icons">settings</i> {{ 'Configure new Url'|trans({}, 'Modules.Bwlauncher.Admin') }}
        </h3>
        <div class="card-body">
            <div class="form-wrapper">
                {{ form_widget(form) }}
            </div>
        </div>
        <div class="card-footer">
            <div class="d-flex justify-content-end">
                <button class="btn btn-primary float-right" id="save-button">
                    {{ 'Save'|trans({}, 'Admin.Actions') }}
                </button>
            </div>
        </div>
    </div>
    {{ form_end(form) }}
{% endblock %}
